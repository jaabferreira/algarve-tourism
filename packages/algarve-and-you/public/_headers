# Cloudflare Pages response headers (https://developers.cloudflare.com/pages/configuration/headers/)

/*
  # Tell browsers to only ever reach this site over HTTPS. Start without
  # `includeSubDomains`/`preload` until every algarveandyou.com subdomain is
  # confirmed HTTPS-only; bump to `max-age=31536000; includeSubDomains; preload`
  # and submit to hstspreload.org once that's verified.
  Strict-Transport-Security: max-age=31536000
